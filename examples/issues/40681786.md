# [40681786] PaymentManifestParser 에러 문자열을 native_error_strings로 이동

**상태: CL 8351543 업로드 (2026-09-03, 리뷰어 chrome-payments-reviews@google.com), 리뷰 대기** — 브랜치 `parser-error-strings-40681786`

## 링크

- crbug: https://crbug.com/40681786 (2020년 rouslan@ 제기, 마지막 활동 594일 전, 연결 CL 0 → 휴면, 직행)
- OSSCA 이슈: 미등록 → 문안 `../drafts/40681786.md` (등록은 사용자)
- Gerrit: https://crrev.com/c/8351543
- 발굴 경로: 1단계 GUIDE(../../steps/1-issue-hunting/GUIDE.md) 경로 2 (코드 상향 — `hunt.py todos components/payments` → triage ✓)

## 이슈 실체

TODO 3개가 한 이슈에 걸려 있는 **시리즈 구조** (40831207과 같은 모양):

1. `payment_manifest_parser.cc:87` — 에러 문자열을 `native_error_strings.cc`로 이동 ← **이번 CL**
2. `payment_manifest_parser.h:92` — 파서 에러를 호출자에게 반환 (이슈의 본론, M급, mojom 관여 가능)
3. `payment_manifest_parser.cc:111` — `default_applications` same-origin 체크를 파서에서 조기 수행 (2번 이후)

이슈 본문: "파서가 devtools 콘솔에만 로그하고 호출자에게 에러를 안 돌려줘서 PaymentRequest.show()
거부 메시지에 상세를 못 붙인다. ... It would also be good to move the error message strings to
native_error_strings.cc." → 문자열 이동은 부수 항목이라 `Fixed:`가 아닌 **`Bug: 40681786`**.

## 구현 (CL 1)

- **핵심은 컨벤션 변환**: 파서는 printf 스타일(`base::StringPrintf("\"%s\" ...", kX)`),
  목적지는 `$N` 플레이스홀더 + `base::ReplaceStringPlaceholders()` (다운로더가 이미 이 패턴).
  printf 포맷을 extern 상수로 빼면 `-Wformat-nonliteral`에 걸리므로 변환이 필수
- `log.Error()` 26곳 → 상수 23개 (`"$1" must be a list.` 는 default/related_applications 공용,
  `"$1" must contain at most $2 entries.` 는 default_applications/supported_origins 공용 —
  후자를 합치며 **"entires" 오타가 함께 고쳐짐**, 커밋 메시지에 명시)
- `%zu` → `base::NumberToString(kMaximumNumberOfItems)` 등
- 헤더/소스 모두 뒤쪽에 **파서 에러 그룹 블록**으로 추가 (헤더가 엄격한 알파벳순이 아니라 SPC 등
  기능별 그룹이 뒤에 붙는 구조임을 확인하고 결정)
- **범위 밖**: `log.Warn()` 13곳(web app icons 경고 등)은 TODO가 "error message"만 말하므로 유지.
  리뷰어가 원하면 후속
- BUILD.gn 변경 없음 (`//components/payments/core` 이미 의존), include 1줄 추가, TODO 1개 제거
- diff: 3파일 +220/−72 근방 → Gerrit **L** 뱃지 예상 (헤더 상수별 주석이 부피의 절반)

## 검증 계획

- 동작 불변 리팩토링 → 기존 `PaymentManifestParserTest` 전원 통과가 검증 (unittest는 에러 문자열
  텍스트를 검증하지 않음)
- 러너: `$OSSCA/examples/scripts/pmp_test.sh` (components_unittests 빌드 + `*ManifestParser*`, 로그 `$OSSCA/steps/4-build-and-test/logs/pmp_*`)
- 업로드 전 origin/main 리베이스 (브랜치는 빌드 트리와 맞추려 8/26 base에서 땀).
  8280660(xuehuichen)이 native_error_strings.cc에 +3 하므로 사소한 충돌 가능

## 리뷰어

- 이번엔 배운 대로 **`chrome-payments-reviews@google.com`** 알리아스로 초기 리뷰 요청

## 커밋 메시지 초안

```
[payments] Move manifest parser error strings to native_error_strings

PaymentManifestParser built its developer console error messages inline
with base::StringPrintf(), while the rest of //components/payments keeps
its native error strings in components/payments/core/native_error_strings
with $N placeholders and base::ReplaceStringPlaceholders().

Move the parser's error messages there, following that convention, so
that they can later be reused when the parser starts returning its
errors to the caller. The messages are unchanged, except that the
"default_applications" and "supported_origins" size limit messages now
share one string, which fixes an "entires" typo in the latter. Warning
messages are left as they are.

Bug: 40681786
```

## 진행 체크리스트

- [x] 발굴 + triage (선점 0 / OSSCA 0 / 594일 휴면) + 코드 진단
- [x] 브랜치 `parser-error-strings-40681786` (8/26 base — 빌드 트리 일치)
- [x] 구현 + `git cl format`
- [x] components_unittests 빌드 (브랜치 base 점프 때문에 50,579스텝 풀빌드, 16h19m) + `*ManifestParser*` **90/90 통과**
- [x] 커밋 `688f4ec7d4786` (webrtc gitlink 제외, 코워커 라인 없음)
- [x] **CL 8351543** 업로드 (09-03, PS1, 리뷰어 알리아스 chrome-payments-reviews@google.com, 8/26 base 그대로 — 로컬 리베이스 회피)
- [x] 기여 기록 `data/contributions/8351543.md` 작성 + 커밋 `8df3d11d` (브랜치 260903-add-8351543)
- [x] 업로드 검증: PS1 서버=로컬 동일 ✓, mergeable True(REBASE_ALWAYS), gwsq 봇이 smcgruer@·nikifork@ 자동 배정
- [x] 기여 기록 브랜치 `260903-add-8351543` → fork(origin) push 완료 (2026-09-05)
- [ ] 기여 기록 PR 생성 (fork → OSSCA-chromium/contributions, 사용자)
- [ ] OSSCA 이슈 등록 (문안 `../drafts/40681786.md`, 사용자)
