# [545843242] PaymentManifestDownloader — rel=payment-method-manifest Link 헤더가 여러 개면 실패해야

**상태: CL 8349386 업로드 (2026-09-04 02:40, 리뷰어 chrome-payments-reviews@), 리뷰 대기**

## 링크

- crbug: https://crbug.com/545843242 (2026-08-13 등록, 마지막 활동 08-14, 연결 CL 0, OSSCA 0 → 직행)
- 스펙: https://w3c.github.io/payment-method-manifest/#fetch-pmm — "Let manifest_links be the result of extracting
  link headers from response whose rel is `payment-method-manifest`. **If manifest_links's size is not 1, then
  return failure.**"
- WPT: `external/wpt/payment-method-manifest/link-header-selection.https.window.js` 서브테스트 5개 중
  "Multiple rel=payment-method-manifest link headers cause fetch to abort"만 [FAIL] (baseline에 기록)
- 이슈 본문에 제보자가 수정 스케치(매칭 링크 수집 → size != 1이면 에러)까지 적어둠

## 현재 코드 (ToT, 8336867 반영 후)

`OnURLLoaderCompleteInternal()` 319~366행: `SplitLinkHeader` 루프 안에서 rel 매칭 → 즉시 URL 검증 →
`InitiateDownload` → **return**. 첫 매칭에서 끝나므로 뒤에 매니페스트 링크가 더 있어도 세지 않음.

## 설계

1. 루프는 **수집만**: rel(소문자화)에 payment-method-manifest가 있으면 `manifest_link_urls.push_back(*link_url)`
   (`ParseLinkHeaderValue` 실패 값은 rel을 알 수 없으니 기존대로 skip)
2. 루프 후 분기: 0개 → 기존 `kNoLinkHeader` / **2개 이상 → 새 에러** / 1개 → 기존 검증(IsValidManifestUrl,
   same-origin)과 `InitiateDownload`를 루프 밖으로 옮겨 그대로 수행
3. 새 상수 `errors::kMultipleLinkHeaders` (native_error_strings, `kNoLinkHeader` 옆):
   `"Multiple \"Link: rel=payment-method-manifest\" HTTP headers found at \"$1\"; exactly one is required."`
4. 스펙대로 **rel 기준으로 개수**를 셈 (URL 유효성은 개수 판정 후 단일 링크에만 적용)
5. 예상 diff: 다운로더 +25/−20(들여쓰기 이동 포함), 에러 문자열 +5, 테스트 +30, baseline −4 → **S~M**

## 테스트 (TDD)

픽스처 `ServerResponse(code, Headers::kSend, link_header, body, net_error)`는 `SetHeader("Link", v)` 한 번 —
실제 HTTP에서 Link 헤더 두 개는 `GetNormalizedHeader`가 쉼표로 합치므로 합쳐진 문자열을 넘기면 동일 상황.
에러 검증은 `EXPECT_CALL(*this, OnManifestDownload(_, kNoContent, "<정확한 메시지>"))` 형식.

1. `MultipleManifestLinkHeadersIsFailure` — `<manifest.json>; rel=payment-method-manifest, <other.json>; rel=payment-method-manifest`
   → 새 에러 메시지로 콜백. **수정 전엔 첫 링크 다운로드가 시작돼 EXPECT_CALL 미충족으로 실패** (빨강 확인용)
2. `MixedCaseDuplicateManifestLinkHeadersIsFailure` — `rel=Payment-Method-Manifest` + `rel=payment-method-manifest`
   → 대소문자만 다른 중복도 2개로 셈 (8336867과 연결)
3. (없으면 추가) `ManifestLinkAmongOtherRelsIsSelected` — `<style.css>; rel=stylesheet, <manifest.json>; rel=payment-method-manifest`
   → 1개로 정상 진행 (WPT "selecting from multiple entries" 대응)
4. WPT baseline `link-header-selection.https.window-expected.txt` 삭제 (유일한 [FAIL]이 이 버그)

## 브랜치·빌드 전략 (풀빌드 회피)

- 브랜치는 **8/26 base(17d2e3e70cb0f)** 에서 → 파서 빌드(같은 base) 증분 재사용
- 그 base엔 8336867 수정이 없으므로 **머지 커밋 `0404b8e003262`를 먼저 cherry-pick** 한 뒤 그 위에 구현
  (업로드 전 `git rebase origin/main` 하면 cherry-pick 커밋은 이미 업스트림에 있어 자동 탈락 → 단일 커밋)
- WPT baseline은 8/26 base에선 옛 이름(`link-header-multiple-manifest-links...`)이라 **리베이스 후 ToT 이름으로 삭제**
  (C++ 빌드·테스트는 8/26 base에서, baseline 삭제는 리베이스 뒤 amend)
- 리뷰어: `chrome-payments-reviews@google.com` 알리아스 (darwin 안내대로)

## 리스크

- **8280660**(xuehuichen, PS9 09-02, 표 없음)이 같은 구간(IsValidManifestUrl/cross-origin 에러 처리)을 재구성 중 →
  뒤에 랜딩하는 쪽이 리베이스. 우리 diff가 작으니 빨리 올리는 게 유리
- 사용자 영향: 지금까지 "여러 매니페스트 링크 중 첫 번째"로 동작하던 사이트가 실패로 바뀜 — 스펙 준수 변경이라
  리뷰어가 받아들일 가능성 높음 (WPT가 이미 그 동작을 요구)

## 커밋 메시지 초안

```
[payments] Fail manifest download on multiple manifest Link headers

The payment method manifest spec requires the payment method identifier
response to carry exactly one Link header with
rel="payment-method-manifest": in "fetch payment method manifest", the
extracted manifest links must have a size of 1, otherwise the fetch
fails. PaymentManifestDownloader instead followed the first matching
Link header and ignored any further ones.

Collect the matching Link headers first and fail the download when there
is more than one, then validate and follow the single remaining link as
before. Add regression tests and delete the WPT baseline that recorded
this failure.

Fixed: 545843242
```

## 진행 체크리스트

- [x] 발굴(545645933 파생) + triage(선점 0) + 코드 진단
- [x] 브랜치 `manifest-link-count-545843242` @ 8/26 base + TEMP 커밋 `e99f5a15d6bd1`(8336867 두 파일 checkout, 리베이스 시 소멸)
- [x] **TDD 빨강**: 새 테스트 3개 중 `MultipleManifestLinksAreFailure`,
      `MultipleManifestLinksWithDifferentCaseAreFailure` **FAILED**(첫 링크로 다운로드 시작 → 콜백 미호출),
      `ManifestLinkAmongOtherLinks`는 수정 전에도 OK(회귀 방지용)
- [x] 구현: 루프는 `std::vector<GURL> manifest_urls` 수집만 → empty→kNoLinkHeader / >1→**kMultipleLinkHeaders**(신규) /
      1→기존 검증·InitiateDownload를 루프 밖으로. 스펙 인용 주석 추가. `git cl format` 클린
- [x] **TDD 초록**: 새 3개 OK, `*ManifestDownloader*:*PaymentManifestParser*` **140/140 통과** (tmux)
- [x] 커밋 `f917f662dac07` (4파일 +88/−30, webrtc gitlink 제외, 코워커 라인 없음)
- [x] 리베이스: TEMP 커밋이 "patch contents already upstream"으로 **자동 drop** ✓ (전략 검증됨) →
      baseline 삭제 + 메시지 보강 → amend `efa1adb569510`
- [x] **CL 8349386** 업로드 (09-04 02:40, PS1, 서버=로컬 동일 ✓, 5파일 +88/−34)
- [ ] OSSCA 등록 → 기여 기록 PR

## 실행 메모

- 테스트를 Claude Code의 Bash 도구로 직접 돌리면 `Check failed: !IsProcessBackgrounded()`로 전부 크래시 — **tmux에서 실행 필수**
  (러너 `$OSSCA/examples/scripts/pmd_wide.sh` 추가)
