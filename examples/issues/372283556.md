# [372283556] crypto/hash 마이그레이션 — services/network 잔여 include 제거

**상태: ✅ 완료** (CL 머지 2026-08-22, 모든 마무리 절차 끝)

## 링크

- crbug: https://crbug.com/372283556 (crypto/hash 마이그레이션 상위 이슈)
- Gerrit CL: https://crrev.com/c/8264232 — **MERGED** (2026-08-22)
- OSSCA 이슈: #282 (마이그레이션 전체 규칙), #307 (services/network 담당분)
- 기여 기록 PR: https://github.com/OSSCA-chromium/contributions/pull/326 — 머지됨, `status: merged` 반영 완료

## 내용

구 해시 API(`crypto/sha2.h`, `crypto/secure_hash.h`)를 새 `crypto/hash` 인터페이스로
마이그레이션하는 작업의 일환. services/network의 실제 호출부는 이미 업스트림에서
마이그레이션이 끝나 있었고, 정리되지 않은 구 API include 9줄이 7개 파일에 남아
있어 제거함 (0 insertions, 9 deletions).

- 로컬 브랜치: `crypto-hash-services-network`
- 리뷰어: ellyjones@ + bashi@ (services/network OWNER)
- 검증: `git grep`으로 사용처 부재 증명 + `services_unittests` 컴파일 + CQ dry run(멘토 실행)

## 진행 체크리스트 (전부 완료)

- [x] OSSCA 이슈 #307에서 services/network 담당 클레임
- [x] 브랜치 생성, include 제거
- [x] 사용처 부재 검증 (grep, `net::SHA256HashValue` 타입은 무관함 확인)
- [x] 업로드 (2026-08-19), 리뷰어 지정, 멘토가 CQ 실행
- [x] 리뷰 통과 → 머지 (2026-08-22)
- [x] 기여 기록 `data/contributions/8264232.md` + PR #326 머지
- [x] `status: merged` 갱신

## 이 사이클에서 배운 것 (다음 CL에 적용)

- 작은 CL로 전체 흐름(이슈→업로드→리뷰→머지→기록)을 한 바퀴 도는 것이 먼저
- include 정리 CL은 "사용처가 없다"를 스스로 증명하는 조사(grep, 타입 구분)가 본 작업
- OWNERS 디렉토리 단위로 CL 범위를 나누고, 활동적인 OWNER를 리뷰어로 지정
- 멘티는 tryjob 권한이 없어 CQ는 멘토/리뷰어가 실행
