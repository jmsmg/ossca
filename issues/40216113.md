# [40216113] Lacros 시절의 중복 `ShouldLoadProfileWithoutWindow` 재검사 제거

**상태: ✅ 완료 — CL 8410085 머지 (2026-09-17 09:29 UTC, PS2, `1e5c4ad3d899d`, dgn@ CQ). 기록 파일 없음 → 8단계에서 `status: merged`로 바로 추가 + `Closes #442` (허가 대기) · 보드 Status `반영 완료`(사용자).**

## 링크

- crbug: https://crbug.com/40216113 (FIXED, 마지막 활동 2023-11-27, 1,023일 경과) → `Bug: 40216113`
- 원 CL: https://crrev.com/c/3548859 (dgn@, 2022-03-29, M102) «Respond to --no-startup-window on process start too»
- Gerrit: **https://crrev.com/c/8410085** (PS1 2026-09-15 05:52 리눅스, 리뷰어 dgn@·nicolaso@, 프리서브밋 경고 0)
- OSSCA 이슈: **#442** (2026-09-15 등록)

## 무엇을 지우나

`chrome/browser/ui/startup/startup_browser_creator_impl.cc` `DetermineURLsAndLaunch()` 초입 5줄:
`ShouldLoadProfileWithoutWindow(*command_line_)`이면 `NOTREACHED()`. 주석 «Remove by M104».
`NOTREACHED()`의 유일한 사용처라 `base/notreached.h` include도 같이. `base/debug/dump_without_crashing.h`는
원 CL이 넣은 `DumpWithoutCrashing()`이 이미 사라져 **지금도 미사용** → 같이 제거.

## 왜 지워도 되나 (호출처 확인)

- `Launch()`에 이르는 `StartupBrowserCreator` 경로는 전부 `LaunchBrowser()` → `IsSilentLaunchEnabled()`를 거치고,
  그 함수가 `kNoStartupWindow`·`kSilentLaunch`·`ShouldLoadProfileWithoutWindow()`를 이미 검사해 early return
- `StartupBrowserCreatorImpl`을 직접 만드는 곳: `app_controller_mac.mm`(`OpenTabsInBrowser`만 호출),
  browsertest 6곳(`Launch()` 호출하지만 자체 dummy command line, 플래그 없음)
- 원 CL 시점의 M104 관찰 기간은 51 마일스톤 전에 끝났고, Lacros 자체가 트리에서 사라짐

## 진행

- [x] 호출처·이력 확인
- [x] 브랜치 `startup-no-window-recheck-40216113` (base 233e625e) + 수정 → 커밋 `d0b436539c379` (09-15 05:30, `git cl format` 변경 없음, AI 흔적 0)
- [x] `unit_tests --gtest_filter='StartupBrowserCreator*'` + 컴파일 (동작 불변, TDD 예외) — ✅ 05:40 빌드 4스텝 58s, **14/14** (러너 `extsvc_force_test.sh startup_nw 'StartupBrowserCreator*'`)
- [x] 리베이스 origin/main(345d761d) 충돌 없음 → `cc96915b87a16` (−8)
- [x] 업로드 ✅ 09-15 **CL 8410085** PS1. 리뷰어 dgn@·nicolaso@ (OWNERS, 최근 6개월 각 69·67커밋), ydago@ 대안
- [x] 6단계 — nicolaso@ +1(09-15) · dgn@ +1(09-16) · 제출 요청 댓글 09-17 06:13(Mac) → dgn@ CQ+2 08:44 → 머지 09:29 UTC (`1e5c4ad3d899d`)
- [x] 7·8단계 기록 PR — ✅ **#463** (09-18, Mac에서 `status: merged`로 작성, `Closes #442`)
- [ ] 보드 Status `반영 완료` (사용자)

## 충돌 예보

파일에 열린 CL 27건(tluk@ include 정리 8327696 등). 이 블록 인접은 없음. origin/main 과 base 사이 이 파일 diff +5줄(다른 위치).

## 이 사이클에서 배운 것

- 빌드 캐시의 base와 브랜치 base가 같으면 `unit_tests`급도 4스텝 58초. 캐시 base를 옮기지 않는 것이 리눅스 박스의 존재 이유다.
- 리뷰어 둘을 OWNERS 중 최근 6개월 활동이 가장 많은 사람으로 고르니 +1이 이틀 안에 모였다. 리눅스 세션이 비활성이어도 제출 요청·기록은 Mac이 이어받을 수 있다 — 단, ossca 저장소에 남긴 기록이 전부다.
