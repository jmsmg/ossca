# [41161335] kSuspendMediaForFrozenFrames 만료 플래그 제거 — WebMediaPlayerImpl

**상태: 3단계 완료(09-18, Mac) — 브랜치 `suspend-frozen-frames-flag-41161335` (base b39241398 = 09-15, 빌드 캐시와 맞춤) 커밋 `654824f078296` (+8/−24, 4파일), gn check OK. **OSSCA #465 등록(09-18).** **4단계 검증 완료(09-19)** — blink_unittests 4h35m/28,621스텝 ✓(덮개 닫힘·배터리 절전으로 실제 경과 ~40h), `WebMediaPlayerImplTest.*` 78/78 ✓. 5단계 업로드 허가 대기.**

## 링크

- crbug: https://crbug.com/41161335 — FIXED (2015년 SPDY 데드락 버그 번호를 frozen-frame 정지 작업이 재사용). 마지막 활동 2025-10
- 도입 CL: https://crrev.com/c/6724838 (dalecurtis@, 2025-07-15, M140) «Suspend WebMediaPlayerImpl instances upon frame frozen»
- Gerrit: (업로드 전)
- OSSCA 이슈: **#465** (2026-09-18, 라벨 2026·chromium-issues, self-assign, Status `멘티 작업 진행 중`)
- 발굴 경로: 코드 상향 — 09-15 «Remove in M<n>» 스윕, 1단계 STATUS 후보 큐 S

## 무엇을 지우나

- `media/base/media_switches.{h,cc}`: 선언 1줄 + 정의·주석 3줄 (`ENABLED_BY_DEFAULT`, «Remove in M143 after it goes stable»)
- `third_party/blink/renderer/platform/media/web_media_player_impl.cc`
  - `OnFrozen()` 경로(:1110): `if (IsEnabled)` 껍데기만 벗기고 본문(프리로드 중단, `was_suspended_for_frame_closed_or_frozen_ = true`, 상태 갱신) 유지
  - `IsPageHidden()`(:3647): `return delegate_->IsPageHidden();` 만 남김
  - `IsFrameHidden()`(:3656): `return is_frame_hidden_;` 만 남김
  - `base::FeatureList`는 다른 플래그로 계속 쓰므로 include 유지
- `web_media_player_impl_unittest.cc` `OnFrozenSuspendsPlayback`: `ScopedFeatureList feature_list(media::kSuspendMediaForFrozenFrames)` 2줄 제거 (다른 테스트가 ScopedFeatureList·media_switches를 쓰므로 include 유지)
- **남기는 것**: `was_suspended_for_frame_closed_or_frozen_` 멤버 — :3306 `… || pending_oneshot_suspend_` 등에서 계속 읽음. 플래그 꺼진 분기에서만 읽던 두 곳(IsPageHidden/IsFrameHidden)만 사라짐
- 등록 없음: about_flags·enums·fieldtrial grep 0 (트리 전체 참조 6줄)

## 착수 전 검증 (1단계 파이프라인)

- ① 선점: `message:"kSuspendMediaForFrozenFrames" status:open` 0
- ② OSSCA 겹침: «SuspendMediaForFrozenFrames» 0 · «41161335» 0
- ③ 같은 파일 열린 CL: `web_media_player_impl.cc` **109건** (hferreiro@ 8396035 «Properly integrate Blink platform media code (2/3)» 등 파일 재배치 시리즈 주의 → 빨리 올린다)
- ④ 리뷰어 생존: dalecurtis@ 09-18 05:07Z · tmathmeyer@ 09-18 00:30Z · liberato@ 09-17 · tguilbert@ 09-18

## 검증 계획

- 타깃 `blink_unittests` (core:unit_tests에 `web_media_player_impl_unittest.cc`). 09-18 blink_unittests 캐시 위 증분 — 단, 브랜치 전환으로 mtime이 바뀐 파일은 다시 컴파일됨
- 필터 `WebMediaPlayerImplTest.*`. 러너 `steps/4-build-and-test/scripts/frozen_frames_test_mac.sh`, 마커 `logs/frozen_done.marker`
- base를 09-15(b39241398)로 잡은 이유: 09-18 fetch로 origin/main은 올라갔지만 third_party는 09-15 상태(gclient sync 전). 캐시·DEPS와 맞추려면 옛 base. 업로드는 Gerrit이 알아서 새 main 위에 올린다

## 리뷰어 (2명)

- dalecurtis@chromium.org — media/OWNERS, 플래그 도입 CL 작성자
- tmathmeyer@chromium.org — media/OWNERS, 최근 가장 활발

## 트레일러

`Bug: 41161335` (이미 FIXED → `Fixed:` 아님)

## 진행 체크리스트

- [x] 1단계 착수 검증 ①~④ (09-18)
- [x] 2단계 OSSCA 이슈 등록 — ✅ **#465** (09-18, `gh issue create`, 본문 1,928자 확인)
- [x] 3단계 브랜치·수정·format·gn check (09-18, +8/−24, 4파일)
- [x] 3단계 커밋 `654824f078296` (09-18). 오브젝트 단독 컴파일은 브랜치 전환으로 mtime이 바뀌어 10분 넘게 걸려 중단 → 4단계 러너의 전체 빌드로 대체
- [x] 4단계 — ✅ blink_unittests 4h35m00s/28,621스텝(mtime 때문에 사실상 전체 재빌드), WebMediaPlayerImplTest.* 78/78 (09-18 14:57 시작 → 09-19~20 사이 완료; 배터리·덮개 닫힘으로 절전 반복)
- [ ] 5단계 업로드 (`-s -T -r dalecurtis@,tmathmeyer@ --hashtag=media`)
- [ ] 7단계 기록 PR
