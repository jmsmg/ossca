# [474398415] kWebCodecsDecoderFlushOptimizations 킬스위치 제거 — WebCodecs 오디오·비디오 디코더

**상태: 4단계 검증 완료(09-21, Mac) — gclient sync ✓ · gn check ✓ · blink_unittests 4h56m/39,600스텝 ✓ · 디코더 테스트 24/24(배치 21 + 단독 3) ✓ · presubmit ✓. 배치의 `DecoderTemplateTest/0.ResetDuringFlush` 크래시는 **main에서도 동일 재현**(테스트 격리 문제, 우리 변경 무관). 브랜치 `webcodecs-flush-killswitch-474398415` 커밋 `607ea058b77cc` (+4/−26, 7파일). OSSCA #478. 5단계 업로드 허가 대기.**

## 링크

- crbug: https://crbug.com/474398415 — VERIFIED(2026-01-26). «When decoding a video, the last few frames are missing»
- 원 수정 CL: https://crrev.com/c/7428252 (dalecurtis@, 2026-01-21, M145; 리뷰 eugene@) «[WebCodecs] Don't always destroy decoders after flush()»
- Gerrit: (업로드 전)
- OSSCA 이슈: **#478** (2026-09-21, 라벨 2026·chromium-issues, self-assign, Status `멘티 작업 진행 중`)
- 발굴 경로: 코드 상향 — 09-15 «Remove after M<n>» 스윕, 1단계 STATUS 후보 큐 S~M

## 무엇을 지우나

- `media/base/media_switches.{h,cc}`: 선언 1줄 + 정의·주석 4줄 («Kill-switch to be removed after M145 stable»)
- `audio_decoder_broker.cc` / `video_decoder_broker.cc` Initialize(): `if (decoder_ && [&& !decoder_factory_needs_update_] && IsEnabled)` → 플래그 항만 제거. 기존 디코더를 `selector_->PrependDecoder()`로 재사용하는 동작 유지. 두 파일 모두 `FeatureList`·`media::k…` 다른 사용 0 → `feature_list.h`·`media_switches.h` include 제거
- `decoder_template.cc` configure() 경로: `IsEnabled ? CreateEOSBuffer(*request->media_config) : CreateEOSBuffer()` → 전자만. `media_switches.h` include 제거(feature_list.h는 원래 include 안 함)
- `video_decoder_broker_test.cc`: `InitWithFeatures({kResolutionBasedDecoderPriority, kWebCodecsDecoderFlushOptimizations}, {})` → `InitAndEnableFeature(kResolutionBasedDecoderPriority)` (다른 플래그는 유지, include 유지)
- `video_decoder_test.cc` `ConfigureGeneratesConfigChangeEOS`: `ScopedFeatureList` 2줄 제거 → 파일에 ScopedFeatureList·media::k 사용 0 → `scoped_feature_list.h`·`media_switches.h` include 제거
- 등록 없음: about_flags·enums·fieldtrial grep 0 (트리 전체 참조 7줄)

## 착수 전 검증 (1단계 파이프라인)

- ① 선점: `message:"kWebCodecsDecoderFlushOptimizations" status:open` 0
- ② OSSCA 겹침: «WebCodecsDecoderFlushOptimizations»·«flush optimizations» 0
- ③ 같은 파일 열린 CL: decoder_template 8건·video_decoder_broker 48건·audio_decoder_broker 1건 — 전부 2023~2026-06의 오래된 리팩터링/리라이트 시리즈, 우리 줄과 무관
- ④ 리뷰어 생존: eugene@ 09-19 · dalecurtis@ 09-18 · tguilbert@ 09-18

## 검증 계획

- **트리 상태**: 사용자가 09-21 main을 `git pull`해 2ca4848로 올라갔고 third_party는 09-15 상태(서브모듈 불일치 52개). 이 브랜치는 새 main 기반이라 **러너가 먼저 `gclient sync -D`**를 돌린 뒤 `gn gen`·`gn check`·빌드. 캐시 대부분 무효 → 큰 빌드 예상(3~5h). 이후로는 트리가 일관되고 M144 랜딩분도 들어와 `unit_tests` 문제도 풀림
- `gn check`는 sync 전엔 «out of scope» 오류(옛 gn 바이너리 + 새 BUILD.gn) → 러너에서 sync 후 실행
- 타깃 `blink_unittests` **`-j 4`**(blink 꼬리 스왑 교훈), 필터 `AudioDecoder*:VideoDecoder*:DecoderTemplate*:DecoderSelector*` (AudioDecoderBrokerTest 3·AudioDecoderTest 3·VideoDecoderBrokerTest 6·VideoDecoderTest 7 + template/selector)
- 러너 `steps/4-build-and-test/scripts/webcodecs_flush_test_mac.sh` (`caffeinate -s -i`, 전원 연결), 마커 `logs/webcodecs_done.marker` (SYNC/GNCHECK/BUILD/TEST)

## 리뷰어 (순차, 09-21 규칙)

- 1차: eugene@chromium.org — webcodecs OWNER, 원 CL 리뷰어, 우리 8410466 CQ 경험
- 2차(1차 +1 뒤): tguilbert@chromium.org 또는 dalecurtis@chromium.org (8423128과 겹치지 않게 tguilbert@ 우선)
- CC: amoseui@chromium.org

## 트레일러

`Bug: 474398415` (VERIFIED)

## 진행 체크리스트

- [x] 1단계 착수 검증 ①~④ (09-21)
- [x] 2단계 OSSCA 이슈 등록 — ✅ **#478** (09-21, `gh issue create`, 본문 1,700자 확인)
- [x] 3단계 브랜치·수정·format·커밋 `607ea058b77cc` (09-21, +4/−26, 7파일). gn check는 sync 후
- [x] 4단계 — ✅ 09-21 sync 4m · gn check OK · blink_unittests 4h56m/39,600스텝 · 디코더 배치 21 OK + 크래시/미실행 3개 단독 실행 3/3 OK · presubmit OK. 배치 크래시는 main 바이너리(baseline 러너)에서도 동일 → 기존 테스트 격리 문제(`audio_decoder_broker_test.cc:223` 전역 SetBinderForTesting)
- [ ] 5단계 업로드 (`-s -T -r eugene@chromium.org --cc amoseui@chromium.org --hashtag=webcodecs`)
- [ ] 7단계 기록 PR

**09-21 4단계 결과** — sync 4m ✓ · gn check ✓ · blink_unittests **4h56m/39,600스텝** ✓ · 디코더 배치 24개 중 **`DecoderTemplateTest/0.ResetDuringFlush` 크래시**(2개 미실행), 나머지 21 OK. 크래시는 `mojo Receiver for media.mojom.InterfaceFactory is already bound` — `audio_decoder_broker_test.cc:223`가 전역 `Platform::Current()->GetBrowserInterfaceBroker()`에 `SetBinderForTesting`으로 등록한 FakeInterfaceFactory가 같은 프로세스 배치의 뒤 테스트(DecoderTemplateTest/0=AudioDecoder)에서 다시 바인드되며 DCHECK. 단독 실행(--single-process-tests x3)은 통과, 배치 재실행 2회는 재현 → **테스트 격리 문제(우리 변경과 무관해 보임)**. main 바이너리로 같은 배치를 돌리는 baseline 러너(`webcodecs_baseline_mac.sh`) 실행 중.

