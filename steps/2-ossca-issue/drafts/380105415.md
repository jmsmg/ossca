제목: [380105415] Remove the kMultipleLcppKeyInitiatorOriginFix kill switch from the LCP critical path predictor

템플릿: **crbug 에서 찾은 이슈 등록** · 라벨 `2026`, `chromium-issues` · Status `멘티 작업 진행 중`

**Bug**

- https://crbug.com/380105415 (Handle opaque initiator origin correctly in double keyed LCPP — FIXED, 2025-03 마지막 활동)

**Documents**

- `chrome/browser/predictors/lcp_critical_path_predictor/lcp_critical_path_predictor_util.cc:1138-1141`: «TODO(crbug.com/380105415): Remove this kill switch after we confirmed that this works fine» 아래 `BASE_FEATURE(kMultipleLcppKeyInitiatorOriginFix, ENABLED_BY_DEFAULT)`. 수정 CL https://crrev.com/c/6038577 (chikamune@, 2024-11-21)이 double-keyed LCPP에서 opaque initiator origin을 올바르게 걸러내도록 고치면서 롤백용으로 넣은 킬스위치
- 사용처는 같은 파일의 `IsValidInitiatorOrigin()` 한 곳. 플래그가 켜지면 opaque·빈 host·localhost·비 https·너무 긴 host를 모두 거르고, 꺼지면 길이만 검사하는 옛 동작
- 도입 후 10개월(약 12 마일스톤) 동안 기본 켜짐이었고 버그는 FIXED로 닫힘. 플래그를 켜고 끄는 테스트 없음, about_flags·enums·fieldtrial 등록 없음

**Description**

- 발견 경로: 코드 상향 — 09-21 «오래된 킬스위치» 스캔(ENABLED_BY_DEFAULT + kill switch 주석 128개 중 제거 의도가 명시된 것)
- 수정: `BASE_FEATURE` 정의와 TODO 삭제, `IsValidInitiatorOrigin()`을 켜진 쪽 분기만 남기고 정리(static const 캐시 변수 제거). 다른 `FeatureList` 사용이 남아 있으면 include 유지. 동작 불변
- 트레일러: `Bug: 380105415` (이미 FIXED)
- 검증: `unit_tests --gtest_filter='LcpCriticalPathPredictor*:*Lcpp*'` + 컴파일
- 선점: 버그 연결 CL은 원 수정과 그 시험용 2건(abandoned)뿐 · 플래그 이름 든 열린 CL 0 · OSSCA 겹침 0. 파일의 열린 CL 8건은 전부 DO-NOT-SUBMIT 실험
- 리뷰어: predictors OWNERS — chikamune@chromium.org(원 CL 작성자, 최근 활동) 먼저, +1 뒤 alexilin@ 등. CC amoseui@

**References**

- https://crsrc.org/c/chrome/browser/predictors/lcp_critical_path_predictor/lcp_critical_path_predictor_util.cc;l=1138
- 원 수정 CL: https://crrev.com/c/6038577
