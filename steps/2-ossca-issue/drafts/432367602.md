제목: [432367602] Remove the expired kMediaStreamAccurateDroppedFrameCount flag from WebMediaPlayerMSCompositor

템플릿: **crbug 에서 찾은 이슈 등록** · 라벨 `2026`, `chromium-issues` · Status `멘티 작업 진행 중`

**Bug**

- https://crbug.com/432367602 (MediaStream: Incorrect dropped frame count in video.getVideoPlaybackQuality() for first frame — **아직 NEW/P2로 열려 있음**. 2025-07-17 등록, 코멘트는 2025-08-02 수정 CL 자동 링크 하나뿐)

**Documents**

- `media/base/media_switches.cc:819-822`: `BASE_FEATURE(kMediaStreamAccurateDroppedFrameCount, ENABLED_BY_DEFAULT)` 위에 «TODO(crbug.com/432367602): Remove after M143». 수정 CL https://crrev.com/c/6766143 (2025-08-01, M140)이 첫 프레임을 드롭으로 잘못 세던 버그를 고치면서 롤백용으로 넣은 플래그
- `third_party/blink/renderer/modules/mediastream/web_media_player_ms_compositor.cc:511, 996`: 사용처 두 곳. `SetCurrentFrame()`에서 «이전 프레임이 있고 렌더되지 않았을 때만» 드롭으로 세는 것과, 렌더 버퍼를 비울 때 `frames_queued()`가 0이면 언더플로하지 않게 `std::max(…, 1) - 1`로 세는 것. 플래그가 꺼진 쪽은 원래(버그 있던) 동작
- 트리는 M155. M143을 12개 마일스톤 지났고, 플래그는 도입 이래 기본 켜짐

**Description**

- 발견 경로: 코드 상향 — 2026-09-15 «Remove … after M<n>» 만료 주석 스윕(97건)에서 XS로 분류, 09-18 착수 검증
- 수정: 플래그 선언·정의 삭제, 두 사용처를 켜진 쪽 분기만 남기고 정리. 컴포지터 파일에서 `base::FeatureList` 사용이 이 둘뿐이라 `base/feature_list.h`(그리고 다른 사용이 없으면 `media/base/media_switches.h`) include도 제거. about_flags·enums.xml·fieldtrial_testing_config 등록 없음(트리 전체 참조 4줄이 전부). 동작 불변
- 트레일러: 원 CL이 `Bug:`로 남겨 둔 버그가 아직 열려 있으므로 이 CL에 **`Fixed: 432367602`** — 플래그 제거로 수정이 확정되는 시점이 맞다
- 검증: `blink_unittests --gtest_filter='WebMediaPlayerMSCompositor*'` (테스트 파일은 드롭 카운트를 직접 검증하지 않아 변경 없음) + `media_unittests` 컴파일. `blink_unittests`는 이 Mac에서 첫 빌드(dry-run 21,814스텝, 2시간 안팎 예상)
- 선점: crbug 연결 CL은 원 수정 1건뿐 · 플래그 이름이 든 열린 CL 0 · OSSCA 겹침 0(«DroppedFrameCount», «432367602»). `media_switches.cc`는 열린 CL 101건의 활발한 파일이라 리베이스는 각오, 6줄 삭제라 충돌 가능성은 낮음. 컴포지터 파일은 열린 CL 9건, 그중 dalecurtis@의 8421647(include 이동)만 근접
- 리뷰어(2명): kron@chromium.org (`web_media_player_ms*` per-file OWNER, 09-17 활동) · mfoltz@chromium.org (media/OWNERS, 원 CL 6766143의 리뷰어·CQ, 09-17 활동)

**References**

- https://crsrc.org/c/media/base/media_switches.cc;l=819
- https://crsrc.org/c/third_party/blink/renderer/modules/mediastream/web_media_player_ms_compositor.cc;l=511
- 원 수정 CL: https://crrev.com/c/6766143
