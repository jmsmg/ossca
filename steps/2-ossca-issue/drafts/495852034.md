제목: [495852034] Remove the kRejectInvalidChildRegions kill switch from HitTestAggregator

템플릿: **crbug 에서 찾은 이슈 등록** · 라벨 `2026`, `chromium-issues` · Status `멘티 작업 진행 중`

**Bug**

- https://crbug.com/495852034 (Viz 히트 테스트에서 검증되지 않은 FrameSinkId로 인한 cross-origin 입력 리디렉션 가능성 — FIXED, P1/S1)

**Documents**

- `components/viz/service/hit_test/hit_test_aggregator.cc:20-21`: «TODO (crbug.com/495852034): Remove once M150 hits Stable» 아래 `BASE_FEATURE(kRejectInvalidChildRegions, ENABLED_BY_DEFAULT)`
- 사용처 한 곳(:165): 자식 영역이 실제로 제출 프레임 싱크의 자식인지 `delegate_->IsChildOf()`로 확인해 아니면 `AggregationError::INVALID_CHILD_REGION`을 돌려주는 검증을 플래그로 감쌈. 플래그가 꺼지면 보안 수정 이전 동작
- 트리는 M156. M150을 6개 마일스톤 지났고 플래그는 도입 이래 기본 켜짐. 테스트·등록 없음

**Description**

- 발견 경로: 코드 상향 — 09-21 «Remove … once M<n>» 만료 주석 재스윕
- 수정: `BASE_FEATURE`·TODO·빈 익명 namespace 삭제, 검증 조건에서 플래그 항 제거, 유일했던 `FeatureList` 사용이 사라지므로 `base/feature_list.h` include 제거. 동작 불변(검증은 이미 항상 켜짐)
- 트레일러: `Bug: 495852034` (FIXED)
- 검증: `viz_unittests --gtest_filter='HitTestAggregator*'` + 컴파일
- 선점: 플래그 이름 든 열린 CL 0 · OSSCA 겹침 0
- 리뷰어: components/viz/service/hit_test OWNERS 중 최근 활동자 1명 먼저, +1 뒤 1명. CC amoseui@

**References**

- https://crsrc.org/c/components/viz/service/hit_test/hit_test_aggregator.cc;l=20
