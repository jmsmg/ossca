제목: [350769496] Remove ChromeShelfPrefs::CleanupPreloadPrefs() now that M127 is long out of stable

템플릿: **crbug 에서 찾은 이슈 등록** · 라벨 `2026`, `chromium-issues` · Status `멘티 작업 진행 중`

**Bug**

- https://crbug.com/350769496 (M127 베타에서 셸프 pref `ShelfDefaultPinLayoutRolls`에 'preload' 값이 중복으로 쌓이던 버그)

**Documents**

- `chrome/browser/ui/ash/shelf/chrome_shelf_prefs.cc:472-493`: «TODO(crbug.com/350769496): Fixes bug from M127 beta, can be removed once M127 is no longer in stable (end of 2024, or mid 2025 is ok)» 아래 `CleanupPreloadPrefs()` — 두 pref 목록의 중복 제거
- 호출처는 `AttachProfile()` 한 곳(:886). 도입 CL `00b457526ab5a`·`4055be54598e2`(Joel Hockey, 2024-07)
- 트리는 M156. 주석이 허용한 «mid 2025»를 1년 넘게 지났다. 중복을 만들던 원인 수정(`OnGetPinPreloadApps()`가 'preload'를 한 번만 씀)은 그대로 남고, 그 회귀 테스트도 유지된다

**Description**

- 발견 경로: 코드 상향 — 09-23 만료 마일스톤 주석 스윕, 리눅스(ChromeOS) 전용 후보 X
- 수정: `.cc` 함수·호출·이제 안 쓰는 `#include <set>`, `.h` 선언, 테스트 `ChromeShelfPrefsTest.CleanupPreloadPrefs` 삭제. 약 −60줄, 3파일
- 트레일러: `Fixed: 350769496` (이 CL로 버그의 남은 일이 끝남 — 버그 상태 확인 후 `Bug:`로 바꿀 수 있음)
- 검증: ChromeOS 빌드 `unit_tests --gtest_filter='ChromeShelfPrefsTest.*'`
- 선점: ⚠️ 같은 파일에 열린 CL 8382427(kGeminiAppPreinstall) — 겹치는 줄은 없지만 먼저 랜딩하면 리베이스 필요 · OSSCA 겹침 0
- 리뷰어: chrome/browser/ui/ash/shelf/OWNERS — khmel@ · diwux@

**References**

- https://crsrc.org/c/chrome/browser/ui/ash/shelf/chrome_shelf_prefs.cc;l=472
