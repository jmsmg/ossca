# CL 8349386 — nikifork@ 스펙 지적 답글 (2026-09-16, 방향 ①: 인정 + 보류 + smcgruer@에게 질의)

게시 위치: nikifork@의 PATCHSET_LEVEL 스레드(09-15 10:56, 미해결)에 Reply. **Resolved 체크하지 않음**(질문이 열려 있음). 상단 Reply → Send.

```
Yeah, you're right. I went back and read the spec instead of the bug this time: the fetch algorithm just walks the Link headers and takes the first rel=payment-method-manifest one ("set manifestURLString ... and break"). Nothing about failing when there's more than one. The "manifest_links's size is not 1" step in the bug isn't in the spec, and index.html hasn't changed since 2019, so it never was. I lifted that quote from the bug without checking it. My bad.

The only thing that argues the other way is WPT. link-header-selection.https.window.js has a subtest "Multiple rel=payment-method-manifest link headers cause fetch to abort" (came in with the 2026-08-25 sync, then consolidated in crrev.com/c/8297921), and we carry it as a known failure. So the test and the spec don't agree with each other.

smcgruer@, since both the bug and the WPT consolidation are yours: is abort-on-multiple something you actually want to change in the spec (then a spec PR should go first and this CL can follow), or is the WPT subtest just wrong (then I'll abandon this and fix the test instead)? Holding the CL until we know.
```

## 근거 (원문 확인 09-16)

- ED https://w3c.github.io/payment-method-manifest/#fetch-pmm : `Let linkHeaders be … For each linkHeader of linkHeaders: … set manifestURLString … and break.` — «size is not 1» 문장 없음
- 스펙 저장소 `index.html` 마지막 커밋 2019-08-12 (b714ff3e)
- crbug 545843242 본문: «Bug report written by jetski. We should verify if this is a spec bug or Chrome bug before fixing.»
- WPT 서브테스트: `link-header-selection.https.window.js:139`, baseline `[FAIL] Multiple rel="payment-method-manifest" link headers cause fetch to abort` — 08-25 wpt sync(6b3a8965) → 08-27 smcgruer@ 통합(46fb4a237cde8, Bug 552832440)

## 이후 분기

- smcgruer@가 «스펙 변경 의도» → 스펙 PR(w3c/payment-method-manifest) 랜딩 뒤 이 CL 재개(리베이스), 설명을 «spec change X에 따라»로 고침
- «WPT가 틀림» → 이 CL abandon(«Abandoned: the spec keeps first-match; the WPT subtest was wrong») + WPT 수정 CL(별건, `external/wpt/…`는 upstream export 됨) — 새 이슈로 등록(허가 후)
- 무응답 1주 → 어텐션 핑 없이 abandon 쪽으로 정리 제안
